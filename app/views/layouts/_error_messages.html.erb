<!--エラーが１つ以上ある場合-->
<!--object=(@user @expense @expense_category)-->
<% if object.errors.any? %>
  <div id="error_explanation">
    <ul>
      <% object.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>